# Pinned to what the results were produced on. README section 12 requires the OpenVINO
# version and export details to be reproducible; the same applies to the retriever,
# which is frozen at stage 5.
#
#   python -m venv .venv
#   .venv\Scripts\python.exe -m pip install -r requirements.txt
#
# Python 3.12.10 on Windows.

# --- inference (stages 6-7): OpenVINO GenAI on NPU / iGPU / CPU
openvino==2026.3.1
openvino-genai==2026.3.1.0
optimum==2.3.0
optimum-intel==2.1.0

# --- retriever (stage 4, frozen at stage 5)
# Embedding model BAAI/bge-small-en-v1.5 is pinned by commit hash in
# src/retrieval/build_index.py, not here.
torch==2.13.0
transformers==5.5.4
tokenizers==0.22.2
faiss-cpu==1.15.0
rank-bm25==0.2.2

# --- corpus preparation (stages 1-3)
pymupdf==1.28.2

# --- analysis (stage 8)
numpy==2.4.6
scipy==1.18.1
scikit-learn==1.9.0
